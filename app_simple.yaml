runtime: python311

env_variables:
  FLASK_ENV: "production"
  FLASK_DEBUG: "False"
  TF_CPP_MIN_LOG_LEVEL: "2"
  MEDIAPIPE_DISABLE_GPU: "1"

automatic_scaling:
  min_instances: 1
  max_instances: 10

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto
  secure: always
